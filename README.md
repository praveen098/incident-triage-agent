# Incident Triage Agent

A RAG-based FastAPI service that suggests triage actions — severity, category, recommended next step — for incoming incident reports by retrieving similar past incidents and prompting an LLM with the retrieved context.

> **Status:** In active development. Target v1: early May 2026.

## Why this exists

Incident triage — deciding how severe an incident is, which team owns it, and what the first response should be — is one of the most time-sensitive steps in an SRE/on-call workflow. Junior responders often spend 5-15 minutes searching past incidents, wikis, and Slack to figure out "have we seen this before?" This project automates that lookup: feed in an incident description, get back a suggested severity, category, and recommended action grounded in similar past incidents.

## Planned stack

- **API:** Python 3.10 + FastAPI
- **Embeddings:** OpenAI `text-embedding-3-small`
- **Vector store:** Chroma (local, in-process, persisted to disk)
- **LLM:** OpenAI `gpt-4o-mini`
- **Corpus:** 30-50 synthetic incidents across categories (outage, security, performance, data integrity, deployment failure)

## Architecture (planned)

```
[Client] ──POST /triage {description}──▶ [FastAPI]
                                             │
                                             ▼
                                      [Embed query]                (OpenAI)
                                             │
                                             ▼
                                 [Retrieve top-k similar]          (Chroma)
                                             │
                                             ▼
                                 [Prompt LLM with context]         (gpt-4o-mini)
                                             │
                                             ▼
                                 Structured response:
                                 {
                                   severity, category,
                                   recommended_action,
                                   similar_incidents: [...]
                                 }
```

A real diagram replaces this ASCII sketch at v1.

## Roadmap

| Day | Date   | Milestone                                                |
|-----|--------|----------------------------------------------------------|
| 18  | Apr 26 | Repo, skeleton README, FastAPI hello-world ✅            |
| 19  | Apr 27 | Project structure + `POST /triage` stub                  |
| 20  | Apr 28 | Embeddings + Chroma vector store + seed corpus           |
| 21  | Apr 29 | LLM integration + end-to-end RAG flow                    |
| 22  | Apr 30 | Test on full corpus, prompt iteration                    |
| 23  | May 1  | README polish, architecture diagram, deploy              |
| 24  | May 2  | Buffer / eval harness                                    |
| 25  | May 3  | v1 ship                                                  |

## Run locally (current state — hello-world only)

```bash
git clone https://github.com/praveen098/incident-triage-agent.git
cd incident-triage-agent
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```

Then visit:
- `http://localhost:8000/` — service info
- `http://localhost:8000/health` — liveness check
- `http://localhost:8000/docs` — interactive Swagger UI (auto-generated by FastAPI)

## What's incomplete

As of Day 18:

- ✅ Repo, README, FastAPI hello-world
- ⬜ `POST /triage` endpoint and request/response schemas
- ⬜ Synthetic incident corpus (30-50 entries)
- ⬜ Embedding + Chroma retrieval
- ⬜ LLM integration with retrieval-augmented prompt
- ⬜ Deployment / demo (Render or Railway)
- ⬜ Eval harness (stretch)

## Author

Praveen Sharma — [github.com/praveen098](https://github.com/praveen098)
